# Backend base URL (no trailing slash). Django example in this repo uses port 8002.
VITE_API_URL=http://127.0.0.1:8002

# Optional: avoid clashing with other local apps
# VITE_DEV_PORT=5174
# VITE_PREVIEW_PORT=4174
