# Backend base URL (no trailing slash). Use your GoDaddy VPS IP/DNS and a free API port.
VITE_API_URL=http://127.0.0.1:8000

# Optional: avoid clashing with other local apps
# VITE_DEV_PORT=5174
# VITE_PREVIEW_PORT=4174
